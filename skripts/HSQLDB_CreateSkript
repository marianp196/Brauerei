CREATE TABLE Bier (
	id INT NOT NULL,
	name VARCHAR(50),
	erstellungsZeitpunkt BIGINT,
	beschreibung VARCHAR(1000),	
	PRIMARY KEY(id)   
);
CREATE TABLE ElementTyp (
	id INT NOT NULL,
	name VARCHAR(50),
	PRIMARY KEY(id)
);

